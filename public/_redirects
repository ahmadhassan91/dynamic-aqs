# Netlify Redirects for Next.js Dynamic AQS CRM

# SPA fallback - redirect all routes to index.html for client-side routing
/*    /index.html   200

# API routes should be handled by Next.js
/api/*  /.netlify/functions/:splat  200

# Redirect HTTP to HTTPS
http://:host/*  https://:host/:splat  301!

# Redirect www to non-www (optional - uncomment if needed)
# https://www.:host/*  https://:host/:splat  301!

# Custom 404 page
/404  /404.html  404
